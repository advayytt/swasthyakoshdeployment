# Everything except the Postgres driver. Use this if psycopg gives you
# trouble: the app runs fully on local SQLite with just these.
#   pip install -r requirements-core.txt

Flask>=3.0.3
Flask-SQLAlchemy>=3.1.1
SQLAlchemy>=2.0.36
python-dotenv>=1.0.1
requests>=2.32.3
Werkzeug>=3.0.3
reportlab>=4.2.2